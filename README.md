# 🎡 Spin Wheel — ระบบลุ้นรางวัล

## โครงสร้างไฟล์

```
spin-wheel/
├── index.html        ← โครงสร้างหลัก (HTML)
├── style.css         ← ธีม Kanit + Gradient
├── script.js         ← ควบคุมฟอร์ม, วงล้อ, ส่งข้อมูล
├── wheelConfig.js    ← ✏️ แก้ของรางวัล / สี / ลิงก์ ที่นี่
└── Code.gs           ← Google Apps Script (วางใน Apps Script)
```

---

## ✅ ขั้นตอน Setup

### 1. ตั้งค่า Google Apps Script

1. เปิด [Google Sheets](https://sheets.google.com) → สร้าง Spreadsheet ใหม่
2. ไปที่ **Extensions → Apps Script**
3. วางโค้ดจาก `Code.gs` ลงไป (แทนที่โค้ดเดิม)
4. กด **Deploy → New Deployment**
   - Type: `Web App`
   - Execute as: `Me`
   - Who has access: `Anyone`
5. กด **Deploy** → Copy URL ที่ได้

### 2. ตั้งค่า wheelConfig.js

เปิดไฟล์ `wheelConfig.js` แล้วแก้ค่าเหล่านี้:

```js
// URL จาก Apps Script
googleScriptUrl: "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec",

// URL Logo บริษัท
logoUrl: "https://your-domain.com/logo.png",

// LINE Add Friend URL
lineAddFriendUrl: "https://line.me/R/ti/p/@YOUR_LINE_ID",
```

### 3. Deploy บน GitHub Pages

1. สร้าง Repository ใหม่บน GitHub
2. อัปโหลดไฟล์ทั้งหมด (ยกเว้น `Code.gs`)
3. ไปที่ **Settings → Pages → Source: main branch**
4. รอสักครู่ → เว็บจะ live ที่ `https://USERNAME.github.io/REPO_NAME`

---

## 🎁 การแก้ไขของรางวัล

เปิด `wheelConfig.js` → แก้ใน `prizes`:

```js
{
  id: "notepad",        // ID unique (ห้ามซ้ำ)
  label: "โน๊ตก้อน",   // ชื่อที่แสดงบนวงล้อและหน้าผลรางวัล
  weight: 50,           // น้ำหนักการออก (มากออกบ่อยกว่า)
  quantity: 60,         // จำนวนของที่มี
  color: "#f9c74f",     // สีบนวงล้อ
  textColor: "#1a1a2e", // สีตัวอักษรบนวงล้อ
  icon: "📓"            // Emoji แสดงบนวงล้อและหน้าผลรางวัล
},
```

**หลักการ weight:**
- `โน๊ตก้อน 60 อัน` → weight: 50
- `เครื่องคิดเลข 10 อัน` → weight: 18
- `พัดลม 10 อัน` → weight: 18
- `แผ่นรองเม้าส์ 2 อัน` → weight: 4
- `ของรางวัลร่วม 5 อัน` → weight: 6
- `Belden` → ปรับตามต้องการ

---

## 📊 ข้อมูลที่บันทึกลง Google Sheets

| Timestamp | ชื่อ | นามสกุล | อีเมล | เบอร์โทร | บริษัท | ตำแหน่ง | ของรางวัลที่ได้ |
|-----------|------|---------|-------|---------|-------|---------|--------------|

---

## 📝 หมายเหตุ

- ระบบส่งข้อมูลแบบ `no-cors` → ไม่แสดง error ถ้า URL ผิด ให้ทดสอบผ่าน Apps Script Logs
- ของรางวัลจะไม่ออกซ้ำเกินจำนวน `quantity` ที่ตั้งไว้
- เมื่อของรางวัลหมด segment นั้นจะหายออกจากวงล้อโดยอัตโนมัติ
